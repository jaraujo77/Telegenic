@using Telegenic.Entities.Models
@using Telegenic.Entities.ViewModels

@model vmDelete<Season>



    <div id="divDeleteButton" class="tg-fieldset-auto-width">
        @using (Html.BeginForm("Delete", "Season", FormMethod.Post, new { @name = "delete_season" }))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.Entity.Id)
            @Html.HiddenFor(m => m.Entity.Series_Id)

            @Html.HiddenFor(m => m.EntityHasRelatedItems, new { Value = (Model.Entity.Episodes != null && Model.Entity.Episodes.Any() ? "True" : "False" ) })

            @*@Html.HiddenFor(m=> m.Entity.Episodes)*@

            @*for (int i = 0; i < Model.Entity.Episodes.Count(); i++)
            {
                @Html.HiddenFor(x=>x.Entity.Episodes.ElementAt(i).EpisodeNumber)
            }*@

            @*@Html.HiddenFor(m => m.Entity.Episodes)*@
            
            @*@Html.Hidden("Entity.Episodes", Newtonsoft.Json.JsonConvert.SerializeObject(Model.Entity.Episodes))*@

            <fieldset>
                <button id="btnDelete"
                        type="submit"
                        class="btn-link tg-btn-link"
                        data-action="delete">
                    &nbsp;Delete
                </button>
            </fieldset>
        }
    </div>